const toggle=document.querySelector('.menu-toggle');const nav=document.querySelector('.mainnav');if(toggle&&nav){toggle.addEventListener('click',()=>{const open=nav.classList.toggle('open');toggle.setAttribute('aria-expanded',String(open));});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');toggle.setAttribute('aria-expanded','false');}));}const year=document.getElementById('year');if(year)year.textContent=new Date().getFullYear();
const contactForm=document.getElementById('hmz-contact-form');if(contactForm){const requiredFields=[...contactForm.querySelectorAll('[required]')];const setValidationMessage=field=>{let message='Pflichtangabe: Bitte füllen Sie dieses Feld aus.';if(field.name==='email'&&(field.validity.typeMismatch||field.validity.patternMismatch))message='Bitte geben Sie eine vollständige gültige E-Mail-Adresse ein, z. B. name@firma.de.';if(field.name==='strasse'&&field.validity.patternMismatch)message='Bitte tragen Sie im Feld Straße nur den Straßennamen ein. Zahlen gehören in das Feld Hausnummer.';if(field.name==='postleitzahl'&&field.validity.patternMismatch)message='Bitte geben Sie eine gültige fünfstellige Postleitzahl ein.';if(field.name==='ort'&&field.validity.patternMismatch)message='Bitte geben Sie im Feld Ort nur Buchstaben und übliche Ortsnamenszeichen ein.';field.setCustomValidity(message);};requiredFields.forEach(field=>{field.addEventListener('invalid',()=>setValidationMessage(field));field.addEventListener('input',()=>field.setCustomValidity(''));});contactForm.addEventListener('submit',event=>{event.preventDefault();requiredFields.forEach(field=>field.setCustomValidity(''));const firstInvalid=requiredFields.find(field=>!field.checkValidity());if(firstInvalid){setValidationMessage(firstInvalid);firstInvalid.scrollIntoView({behavior:'smooth',block:'center'});setTimeout(()=>{firstInvalid.focus({preventScroll:true});firstInvalid.reportValidity();},350);return;}const data=new FormData(contactForm);const body=['Vorname: '+data.get('vorname'),'Name: '+data.get('name'),'Telefonnummer: '+data.get('telefon'),'E-Mail-Adresse: '+data.get('email'),'Straße: '+data.get('strasse'),'Hausnummer: '+data.get('hausnummer'),'Postleitzahl: '+data.get('postleitzahl'),'Ort: '+data.get('ort'),'Etage: '+data.get('etage'),'Lage der Wohnung: '+(data.get('lage')||'nicht angegeben'),'Wohnungsnummer: '+(data.get('wohnungsnummer')||'nicht angegeben'),'Kundennummer: '+(data.get('kundennummer')||'keine Angabe'),'','Anliegen:',''+data.get('anliegen')].join('\n');window.location.href='mailto:info@hmz-juestel.de?subject='+encodeURIComponent('Kontaktanfrage über die HMZ-Webseite')+'&body='+encodeURIComponent(body);});}
